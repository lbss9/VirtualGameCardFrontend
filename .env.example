# URL base da API VirtualGameCard (backend .NET)
VITE_API_URL=http://localhost:8090

# true: usa dados locais para revisar todas as telas sem o backend
# false: usa exclusivamente a API definida acima
VITE_USE_MOCKS=false
